---
tags: [session, analysis, vault-creation]
date: 2026-05-03
---

# 2026-05-03 — Анализ проекта и создание vault

## Что сделано

- Проанализированы все Swift-файлы проекта (9 файлов)
- Создана полная структура Obsidian vault в `dropShelf_ob/`
- Задокументированы архитектура, стек, паттерны, решения, баги

## Ключевые находки

### Архитектура
- Приложение использует гибридный подход: SwiftUI только для `@main`, вся UI — AppKit
- DnD реализован в двух местах: `DropTargetView` и `OverlapStackView` — потенциальное дублирование

### Технический долг
- `NSLog` debug-вызовы в `DropTargetView` (5 строк) — нужно убрать перед релизом
- Два ассета для иконки статус-бара: `StatusBarIcon` и `statusbar` — стоит унифицировать
- `canBecomeKey/Main = true` в `FloatingPanelWindow` при `.nonactivatingPanel` — противоречие

### git status
- `ShelfViewController.swift` помечен как изменённый (M) — что меняется не ясно из сессии

## Созданные заметки

**00-home**: index, текущие приоритеты  
**atlas**: архитектура, стек, деплой  
**integrations**: Drag & Drop API, NSStatusItem, Mouse Shake Detection  
**decisions**: NSPanel vs NSWindow, OverlapStackView DnD  
**debugging**: дублирование DnD, StatusBarIcon  
**patterns**: глобальный монитор событий, layer-эффекты, snapshot  
**business**: продуктовая идея
